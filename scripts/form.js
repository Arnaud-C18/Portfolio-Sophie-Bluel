/*import { currentlyDisplayImage } from "./modal.js";

const addImageForm = document.querySelector("#addImageForm");
addImageForm.addEventListener("change", formValidation);

function formValidation() {
    const titleRegex = /[A-Za-z.&",-\d\s]{2,40}/;
    if (currentlyDisplayImage) {
        console.log("ca marche")
    }
}

let formProject = new FormData();
*/
